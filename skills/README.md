# Skills

This directory stores skill packages.

Each skill should follow:

```text
skills/<skill-folder>/
├── SKILL.md
├── agents/openai.yaml
├── scripts/        # optional
├── references/     # optional
└── assets/         # optional
```

Catalog:

- `skills/INDEX.md` (generated)
